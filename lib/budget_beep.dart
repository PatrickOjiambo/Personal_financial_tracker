import 'package:flutter/material.dart';

class BudgetBeep extends StatelessWidget {
  const BudgetBeep({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}